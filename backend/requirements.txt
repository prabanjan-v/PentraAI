# ── Web framework ──────────────────────────────────────────
# Updated for Python 3.13 pre-built wheel support
fastapi==0.115.4
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── LLM providers ───────────────────────────────────────────
# Groq removed. PentraAI now uses OpenAI-compatible providers
# (DeepSeek via OpenCode Zen + Gemini) through the openai SDK.
openai==1.59.6
# Legacy — no longer used by llm.py, kept for back-compat. Safe to delete.
google-generativeai==0.8.3
anthropic==0.39.0

# ── HTTP & browser automation ───────────────────────────────
httpx==0.27.2
playwright==1.49.0          # Python 3.13 wheels available
beautifulsoup4==4.12.3
lxml==5.3.0                 # Python 3.13 wheels available

# ── Security / JWT ──────────────────────────────────────────
PyJWT==2.9.0
python-jose[cryptography]==3.3.0

# ── GraphQL ─────────────────────────────────────────────────
gql==3.5.0
aiohttp==3.11.10            # Python 3.13 wheels available

# ── Agent framework ─────────────────────────────────────────
langgraph==0.2.53
langchain-core==0.3.21

# ── Async / queue ───────────────────────────────────────────
celery==5.4.0
redis==5.2.1

# ── Storage ─────────────────────────────────────────────────
sqlalchemy==2.0.36
aiosqlite==0.20.0

# ── Utilities ───────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.3            # Python 3.13 wheels available
pydantic-settings==2.7.0
httpx[http2]==0.27.2